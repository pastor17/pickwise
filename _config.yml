# This site serves pre-built Hugo HTML.
# Jekyll should not process any files — just serve them as-is.
theme: null
exclude: ['src', '.github', '.gitignore', '_config.yml']
collections: []
defaults:
  - scope:
      path: ''
    values:
      layout: null
show_drafts: false
future: false
